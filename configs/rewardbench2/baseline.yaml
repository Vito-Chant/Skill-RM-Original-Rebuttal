data_source: ${SKILLRM_DATA_ROOT}/rewardbench_v2/rewardbench_v2.jsonl
output_dir: runs/baseline/rewardbench2
evaluation_mode: official_compat
model: ${SKILLRM_MODEL}
base_urls: ${SKILLRM_BASE_URLS}
seed: 0
workers: 80
include_ties: true
score_w_ratings: false
resume: true
temperature: 0.0
top_p: 1.0
max_tokens: 4096
enable_thinking: false
send_thinking_field: true
save_reasoning: false
timeout: 300
retries: 2
progress_every: 25
setting_name: baseline

