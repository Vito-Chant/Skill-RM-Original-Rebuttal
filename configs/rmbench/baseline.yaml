benchmark: rmbench
data_source: ${SKILLRM_DATA_ROOT}/rmbench/rmbench.jsonl
output_dir: runs/baseline/rmbench
evaluation_mode: pairwise_baseline
model: ${SKILLRM_MODEL}
base_urls: ${SKILLRM_BASE_URLS}
seed: 0
workers: 160
resume: true
temperature: 0.0
top_p: 1.0
max_tokens: 4096
enable_thinking: false
send_thinking_field: false
save_reasoning: false
timeout: 300
retries: 2
progress_every: 100
setting_name: baseline

