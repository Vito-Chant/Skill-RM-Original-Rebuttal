# RewardBench2 Principles

Treat subset metadata as routing signal only in operational setting. Do not infer hidden chosen labels.
For ground-truth annotated samples, compare candidate claims against the visible ground truth rather than against candidate style.
