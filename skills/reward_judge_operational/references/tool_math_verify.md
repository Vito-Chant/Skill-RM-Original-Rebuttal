# Math Verification Tool Slot

When a visible math reference exists, compare final answers symbolically or by normalized numeric value.
If no executable verifier is configured, still extract final answers and check consistency before judging style.
