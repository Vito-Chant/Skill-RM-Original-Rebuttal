# Hard Verifier Protocol

Use hard verifier outputs as decisive evidence only when they compare visible response content to visible references, checklists, test cases, or answer keys.
Do not use hidden labels, chosen/rejected fields, or benchmark gold labels as verifier inputs.
